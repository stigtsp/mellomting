# build artifacts
/bin/
/dist/
*.exe
# `go build ./cmd/...` drops the binary in the repo root
/mellomting

# tests / coverage
*.test
coverage.*

# editor / OS
.DS_Store

# local test credentials (never commit)
opencode-for-testing-dont-commit.json
